%h2 Click on the white squares to seed the grid with live cells
%form
	%table{:name => 'grid', :border => '1'}
		- (0...@rows).each do |row|
			%tr
				- (0...@columns).each do |column|
					%td
	%input{:type => 'submit', :value => 'Seed Grid'}